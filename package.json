{
  "name": "openwebui-openai-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "check": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/bun": "latest"
  }
}
